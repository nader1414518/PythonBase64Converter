# PythonBase64Converter

you need to install Pillow if it's not already installed

pip3 install pillow

and then replace the image string with the one you want to decode and then run the main.py script
